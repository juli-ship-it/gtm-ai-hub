// Debug template creation issues
const { createClient } = require('@supabase/supabase-js')

// Test database connection
async function testTemplateCreation() {

  try {
    // This would need the actual Supabase URL and key
    // const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY)policies blocking insert')

  } catch (error) {

  }
}

testTemplateCreation()
